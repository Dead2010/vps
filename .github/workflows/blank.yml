name: Bore VPS

on: 
  workflow_dispatch:

jobs:
  vps:
    runs-on: ubuntu-latest
    steps:

    - name: Update system
      run: |
        sudo apt update

    - name: Install SSH
      run: |
        sudo apt install -y openssh-server curl
        sudo mkdir -p /var/run/sshd

    - name: Set runner password
      run: |
        echo "runner:12345678" | sudo chpasswd

    - name: Enable password login
      run: |
        sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/g' /etc/ssh/sshd_config
        sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/g' /etc/ssh/sshd_config
        sudo service ssh restart

    - name: Install bore
      run: |
        curl -L https://github.com/ekzhang/bore/releases/latest/download/bore-x86_64-unknown-linux-musl -o bore
        chmod +x bore
        sudo mv bore /usr/local/bin/

    - name: Start Bore Tunnel
      run: |
        bore local 22 --to bore.pub > bore.log 2>&1 &
        sleep 5
        cat bore.log

    - name: Show SSH Login
      run: |
        echo "=============================="
        echo "SSH USER: runner"
        echo "SSH PASSWORD: 12345678"
        echo "Check above for bore.pub port"
        echo "Example:"
        echo "ssh runner@bore.pub -p PORT"
        echo "=============================="

    - name: Keep VPS Alive
      run: |
        sleep 6h